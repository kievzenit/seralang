package test_package;

func main() int {
    let a = 0;

    if (foo() && bar()) {
        a = 1;
    } else if (foo() || bar() && foobar()) {
        a = 2;
    } else if (foo()) {
        a = 3;
    } else if (bar()) {
        a = 4;
    } else if (foobar()) {
        a = 5;
    } else {
        a = 6;
    }

    return a;
}

func foobar() bool {
    return false || true;
}

func bar() bool {
    return false;
}

func foo() bool {
    static let b = 3;
    let a = b ^ 5 - 1 <= b * 2 && 5 * b & 1 > 1 | 2 * 1 && b == b || b != b;
    return a;
}