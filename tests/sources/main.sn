package test_package;

func main() int {
    let a = 0;

    while (true) {
        if (a > 20) {
            break;
        } else if (a > 10) {
            a += 2;

            loop {
                if (a < 15) {
                    a += 1;
                    continue;
                }

                if (a == 15) {
                    loop {
                        a += 2;
                        while (a < 30) {
                            do {
                                if (a == 15) {
                                    a += 1;
                                }

                                break 2:uint;
                            } while (true);
                        }

                        if (a >= 30 && a != 1 && a != 2) {
                            breakall;
                        }

                        continue;
                    }

                    break 2:uint;
                }
            }
        } else {
            a += 1;
        }

        a += 1;
    }

    return a;
}
